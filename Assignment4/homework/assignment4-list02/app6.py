list  = [1, 2, 3, 4, 5]
# Takes the length of the list and minuses 1 since they are zero-indexed (we start counting at 0)
print(list[len(list) - 1]) #-1 means back se start hoga

# The line below works too!!
# print(lst[-1]) 