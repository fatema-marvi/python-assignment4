def main():
    curr_value = 1  # Start with an initial value
    while curr_value < 100:
        curr_value = curr_value * 2
        print(curr_value)  # Print the current value of curr_value

main()  # Call the function
